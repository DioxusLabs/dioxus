{
  // Disable toml formatting since everyone uses it differently
  "[toml]": {
    "editor.formatOnSave": false
  },
  // // Make sure we turn on features so we actually get intellisense
  // "rust-analyzer.cargo.features": [
  //   "server",
  //   "web",
  //   "fullstack",
  //   "desktop",
  //   "mobile",
  // ],
  // don't check tests since that adds lots of comp time
  "rust-analyzer.check.allTargets": false,
  // don't format with dioxus since we want the autoformatting tests to take invalid inputs
  "dioxus.formatOnSave": "disabled",
}
