[package]
name = "geolocation-demo"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dioxus = { workspace = true }
dioxus-mobile-geolocation = { path = "../../../packages/mobile-geolocation", features = ["location-fine"] }

[features]
default = ["mobile"]
server = ["dioxus/server"]
web = ["dioxus/web"]
mobile = ["dioxus/mobile"]