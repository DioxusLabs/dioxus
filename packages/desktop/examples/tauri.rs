fn main() {
    tauri::AppBuilder::default().setup(move |app, name| {
        //
        let window = app.get_window();

        let window = app.get_window();
        tauri::spawn(|| async move {
            //
        });
    });
}
