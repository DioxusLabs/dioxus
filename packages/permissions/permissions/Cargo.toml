[package]
name = "permissions"
version = "0.7.0-rc.3"
edition = "2021"
description = "Cross-platform permission management system with linker-based collection"
authors = ["DioxusLabs"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/DioxusLabs/dioxus"
homepage = "https://dioxuslabs.com"
documentation = "https://docs.rs/permissions"
keywords = ["permissions", "mobile", "desktop", "web", "cross-platform"]
categories = ["development-tools::build-utils"]

[dependencies]
permissions-core = { path = "../permissions-core" }
permissions-macro = { path = "../permissions-macro" }
const-serialize = { path = "../../const-serialize" }

[dev-dependencies]
