{
  "name": "cloudflare-workers-hello-world",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "dx build --platform web --features web --skip-assets",
    "serve": "wrangler pages dev --compatibility-date 2024-01-29 --proxy 8080 --script-path dist/worker/shim.mjs -- dx serve --platform web --features web",
    "build:server": "cargo install -q worker-build && worker-build --features server && ncp build/ dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ncp": "^2.0.0",
    "wrangler": "^3.28.1"
  }
}
