{
  "name": "cloudflare-workers-hello-world",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "wrangler pages dev --compatibility-date 2024-01-29 --proxy 8080 --script-path dist/_worker.js -- dx serve --platform web --features web",
    "build:server": "cargo install -q worker-build && worker-build --dev --features server && ncp build/ dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "ncp": "^2.0.0",
    "wrangler": "^3.28.1"
  }
}
