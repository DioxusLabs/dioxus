pub struct Watcher {}

impl Watcher {
    // Spawns a new notify instance that alerts us of changes
    pub fn new() -> Self {
        Self {}
    }

    /// Subsribe to the watcher
    pub fn subscribe() {}
}
