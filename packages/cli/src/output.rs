//! Our pretty printer for builds, serve, bundles, and other things
//! Is basically just an inline viewport + logging with some interactivity
//! The idea is to be similar to a traditional `cargo run` with a pinned bottom bar, but also
//! with more functionality.
