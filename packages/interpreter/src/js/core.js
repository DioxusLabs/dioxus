function setAttributeInner(node,field,value,ns){if(ns==="style"){node.style.setProperty(field,value);return}if(ns){node.setAttributeNS(ns,field,value);return}switch(field){case"value":if(node.tagName==="OPTION")setAttributeDefault(node,field,value);else if(node.value!==value)node.value=value;break;case"initial_value":node.defaultValue=value;break;case"checked":node.checked=truthy(value);break;case"initial_checked":node.defaultChecked=truthy(value);break;case"selected":node.selected=truthy(value);break;case"initial_selected":node.defaultSelected=truthy(value);break;case"dangerous_inner_html":node.innerHTML=value;break;case"style":let existingStyles={};for(let i=0;i<node.style.length;i++){let prop=node.style[i];existingStyles[prop]=node.style.getPropertyValue(prop)}node.setAttribute(field,value);for(let prop in existingStyles)if(!node.style.getPropertyValue(prop))node.style.setProperty(prop,existingStyles[prop]);break;case"multiple":if(setAttributeDefault(node,field,value),node.options!==null&&node.options!==void 0){let options=node.options;for(let option of options)option.selected=option.defaultSelected}break;default:setAttributeDefault(node,field,value)}}function setAttributeDefault(node,field,value){if(!truthy(value)&&isBoolAttr(field))node.removeAttribute(field);else node.setAttribute(field,value)}function truthy(val){return val==="true"||val===!0}function isBoolAttr(field){switch(field){case"allowfullscreen":case"allowpaymentrequest":case"async":case"autofocus":case"autoplay":case"checked":case"controls":case"default":case"defer":case"disabled":case"formnovalidate":case"hidden":case"ismap":case"itemscope":case"loop":case"multiple":case"muted":case"nomodule":case"novalidate":case"open":case"playsinline":case"readonly":case"required":case"reversed":case"selected":case"truespeed":case"webkitdirectory":return!0;default:return!1}}function isTemplateSlotSentinel(x){return x!==null&&typeof x==="object"&&x.__dxTemplateSlot===!0}function isVirtual(x){return x!==null&&typeof x==="object"&&x.__virtual===!0}function resolveAnchorBefore(before){while(before&&isVirtual(before)){if(before.materialized&&before.materialized.isConnected)return before.materialized;before=before.before}return before}class BaseInterpreter{global;local;root;handler;resizeObserver;intersectionObserver;nodes;stack;templates;m;constructor(){}initialize(root,handler=null){this.global={},this.local={},this.root=root,this.nodes=[root],this.stack=[root],this.templates={},this.handler=handler,root.setAttribute("data-dioxus-id","0")}handleResizeEvent(entry){let target=entry.target,event=new CustomEvent("resize",{bubbles:!1,detail:entry});target.dispatchEvent(event)}createResizeObserver(element){if(!this.resizeObserver)this.resizeObserver=new ResizeObserver((entries)=>{for(let entry of entries)this.handleResizeEvent(entry)});this.resizeObserver.observe(element)}removeResizeObserver(element){if(this.resizeObserver)this.resizeObserver.unobserve(element)}handleIntersectionEvent(entry){let target=entry.target,event=new CustomEvent("visible",{bubbles:!1,detail:entry});target.dispatchEvent(event)}createIntersectionObserver(element){if(!this.intersectionObserver)this.intersectionObserver=new IntersectionObserver((entries)=>{for(let entry of entries)this.handleIntersectionEvent(entry)});this.intersectionObserver.observe(element)}removeIntersectionObserver(element){if(this.intersectionObserver)this.intersectionObserver.unobserve(element)}createListener(event_name,element,bubbles){if(event_name=="resize")this.createResizeObserver(element);else if(event_name=="visible")this.createIntersectionObserver(element);if(bubbles)if(this.global[event_name]===void 0)this.global[event_name]={active:1,callback:this.handler},this.root.addEventListener(event_name,this.handler);else this.global[event_name].active++;else{let id=element.getAttribute("data-dioxus-id");if(!this.local[id])this.local[id]={};element.addEventListener(event_name,this.handler)}}removeListener(element,event_name,bubbles){if(event_name=="resize")this.removeResizeObserver(element);else if(event_name=="visible")this.removeIntersectionObserver(element);else if(bubbles)this.removeBubblingListener(event_name);else this.removeNonBubblingListener(element,event_name)}removeBubblingListener(event_name){if(this.global[event_name].active--,this.global[event_name].active===0)this.root.removeEventListener(event_name,this.global[event_name].callback),delete this.global[event_name]}removeNonBubblingListener(element,event_name){let id=element.getAttribute("data-dioxus-id");if(delete this.local[id][event_name],Object.keys(this.local[id]).length===0)delete this.local[id];element.removeEventListener(event_name,this.handler)}removeAllNonBubblingListeners(element){let id=element.getAttribute("data-dioxus-id");delete this.local[id]}getNode(id){return this.nodes[id]}pushRoot(node){this.stack.push(node)}createVirtualAnchor(parent,before){return{__virtual:!0,parent,before}}resolveAnchor(anchor){return resolveAnchorBefore(anchor.before)}materializeAnchor(anchor,items){for(let it of items)if(!isVirtual(it)){anchor.materialized=it;break}}appendChildren(id,many){let root=this.nodes[id],els=this.stack.splice(this.stack.length-many);this.applyChunk(els,root,null)}applyChunk(items,parent,cursorBefore){let cursor=cursorBefore;for(let i=items.length-1;i>=0;i--){let it=items[i];if(isTemplateSlotSentinel(it))continue;if(isVirtual(it))it.parent=parent,it.before=cursor;else cursor=it}for(let i=0;i<items.length;i++){let it=items[i];if(isTemplateSlotSentinel(it)||isVirtual(it))continue;if(cursorBefore)parent.insertBefore(it,cursorBefore);else parent.appendChild(it)}}recordTemplateSlots(parent,items){let hasSlot=!1;for(let it of items)if(isTemplateSlotSentinel(it)){hasSlot=!0;break}if(!hasSlot)return;if(!parent.__dxLocalSlots)parent.__dxLocalSlots=[];let templateIdx=0;for(let it of items){if(isTemplateSlotSentinel(it))parent.__dxLocalSlots.push(templateIdx);templateIdx++}}finalizeTemplateRoots(roots){let result=[];for(let root of roots){if(isTemplateSlotSentinel(root)){result.push(null);continue}if(isVirtual(root)){result.push(null);continue}let r=root,slotPaths=[],slotMap=new Map;if(this.gatherTemplateSlots(r,[],slotPaths,slotMap),slotPaths.length>0)r.__dxSlotPaths=slotPaths,r.__dxSlotMap=slotMap;result.push(r)}return result}gatherTemplateSlots(node,pathPrefix,slotPaths,slotMap){let el=node,localSlots=el.__dxLocalSlots;if(localSlots&&localSlots.length>0){let key=pathPrefix.join(","),set=slotMap.get(key);if(!set)set=new Set,slotMap.set(key,set);for(let idx of localSlots)set.add(idx),slotPaths.push([...pathPrefix,idx]);el.__dxLocalSlots=void 0}if(!node.firstChild)return;let slotSet=localSlots?new Set(localSlots):null,realChild=node.firstChild,templateIdx=0;while(realChild){while(slotSet&&slotSet.has(templateIdx))templateIdx++;this.gatherTemplateSlots(realChild,[...pathPrefix,templateIdx],slotPaths,slotMap),realChild=realChild.nextSibling,templateIdx++}}replaceWithChunk(id,m){let root=this.nodes[id],items=this.stack.splice(this.stack.length-m);if(isVirtual(root))this.applyChunk(items,root.parent,this.resolveAnchor(root)),this.nodes[id]=void 0;else{let node=root;if(node.listening)this.removeAllNonBubblingListeners(node);let{parentNode:parent,nextSibling:next}=node;node.remove(),this.applyChunk(items,parent,next)}}insertChunkAfter(id,m){let root=this.nodes[id],items=this.stack.splice(this.stack.length-m);if(isVirtual(root)){this.applyChunk(items,root.parent,this.resolveAnchor(root));for(let i=0;i<items.length;i++){let it=items[i];if(!isVirtual(it)){root.before=it;break}}}else{let node=root;this.applyChunk(items,node.parentNode,node.nextSibling)}}insertChunkBefore(id,m){let root=this.nodes[id],items=this.stack.splice(this.stack.length-m);if(isVirtual(root))this.applyChunk(items,root.parent,this.resolveAnchor(root));else{let node=root;this.applyChunk(items,node.parentNode,node)}}setNodeText(id,text){let node=this.nodes[id];if(isVirtual(node)){if(text==="")return;let t=document.createTextNode(text),insertBefore=this.resolveAnchor(node);if(node.parent)if(insertBefore)node.parent.insertBefore(t,insertBefore);else node.parent.appendChild(t);node.materialized=t,this.nodes[id]=t;return}node.textContent=text}removeNode(id){let node=this.nodes[id];if(node===void 0)return;if(isVirtual(node)){this.nodes[id]=void 0;return}let el=node;if(el.listening)this.removeAllNonBubblingListeners(el);node.remove()}replacePlaceholderPath(ptr,len,m){let items=this.stack.splice(this.stack.length-m),node=this.loadChild(ptr,len);this.replaceAtResolvedTarget(node,items)}replaceAtResolvedTarget(target,items){if(isVirtual(target)){let parent=target.parent,cursor=this.resolveAnchor(target);this.applyChunk(items,parent,cursor),this.materializeAnchor(target,items)}else{let real=target,parent=real.parentNode,next=real.nextSibling;real.remove(),this.applyChunk(items,parent,next)}}walkSlotPath(root,step,len,treatLeafAsSlot){let{__dxSlotMap:slotMap,__dxSlotAnchors:slotAnchors}=root,node=root,prefixParts=[];for(let i=0;i<len;i++){let s=step(i),slotsHere=slotMap?.get(prefixParts.join(","));if(slotsHere&&slotsHere.has(s)){if(treatLeafAsSlot&&i===len-1){prefixParts.push(s);let anchor=slotAnchors?.get(prefixParts.join(","));if(!anchor)throw Error("loadChild: missing slot anchor");return anchor}if(treatLeafAsSlot)throw Error("loadChild: byte path traverses a slot position")}let slotsBefore=0;if(slotsHere){for(let slot of slotsHere)if(slot<s)slotsBefore++}let realStep=s-slotsBefore,child=node.firstChild;while(realStep>0&&child)child=child.nextSibling,realStep--;if(!child)return null;node=child,prefixParts.push(s)}return node}loadChild(ptr,len){let root=this.stack[this.stack.length-1];return this.walkSlotPath(root,(i)=>this.m.getUint8(ptr+i),len,!0)}loadChildBytes(array){let root=this.stack[this.stack.length-1];return this.walkSlotPath(root,(i)=>array[i],array.length,!0)}replacePlaceholderPathBytes(array,n){let items=this.stack.splice(this.stack.length-n),target=this.loadChildBytes(array);this.replaceAtResolvedTarget(target,items)}prepareTemplateClone(tmpl_id,index){let saved=this.templates[tmpl_id][index],clone=saved.cloneNode(!0),slotMap=saved.__dxSlotMap,slotPaths=saved.__dxSlotPaths;if(slotMap)clone.__dxSlotMap=slotMap;if(slotPaths)clone.__dxSlotPaths=slotPaths;if(slotPaths&&slotPaths.length>0)clone.__dxSlotAnchors=this.buildSlotAnchors(clone,slotPaths,slotMap);return clone}buildSlotAnchors(cloneRoot,slotPaths,slotMap){let anchors=new Map,byParent=new Map;for(let path of slotPaths){if(path.length===0)continue;let parentKey=path.slice(0,-1).join(","),key=path.join(","),leaf=path[path.length-1],list=byParent.get(parentKey);if(!list)list=[],byParent.set(parentKey,list);list.push({key,path,leaf})}for(let[parentKey,slotList]of byParent.entries()){let parentPath=parentKey===""?[]:parentKey.split(",").map(Number),parentNode=this.walkSlotPath(cloneRoot,(i)=>parentPath[i],parentPath.length,!1);if(!parentNode)continue;let parentSlots=slotMap.get(parentKey);if(!parentSlots)continue;slotList.sort((a,b)=>a.leaf-b.leaf);for(let i=slotList.length-1;i>=0;i--){let{key,leaf}=slotList[i],slotsBefore=0;for(let s of parentSlots)if(s<leaf)slotsBefore++;let realBeforeIdx=leaf-slotsBefore,realChildren=parentNode.childNodes,nextReal=realBeforeIdx<realChildren.length?realChildren[realBeforeIdx]:null,nextSlot=slotList[i+1],nextAnchor=nextSlot?.leaf===leaf+1?anchors.get(nextSlot.key)??null:null,anchor=this.createVirtualAnchor(parentNode,nextAnchor??nextReal);anchors.set(key,anchor)}}return anchors}saveTemplate(nodes,tmpl_id,slotPaths){if(this.templates[tmpl_id]=nodes,!slotPaths||slotPaths.length===0)return;let perRoot=nodes.map(()=>[]);for(let i=0;i<slotPaths.length;i++){let p=slotPaths[i];if(p.length===0)continue;let rootIdx=p[0];if(rootIdx>=perRoot.length)continue;let rel=Array(p.length-1);for(let k=1;k<p.length;k++)rel[k-1]=p[k];perRoot[rootIdx].push(rel)}for(let r=0;r<nodes.length;r++){let root=nodes[r],paths=perRoot[r];if(!root||paths.length===0)continue;root.__dxSlotPaths=paths;let map=new Map;for(let path of paths){if(path.length===0)continue;let key=path.slice(0,-1).join(","),set=map.get(key);if(!set)set=new Set,map.set(key,set);set.add(path[path.length-1])}root.__dxSlotMap=map}}setAttributeInner(node,field,value,ns){setAttributeInner(node,field,value,ns)}}export{BaseInterpreter};
